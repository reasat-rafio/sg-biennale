import { FilterIcon } from "@components/icons/filter";

interface IndexProps {}

export const FilteringMobileSection: React.FC<IndexProps> = ({}) => {
  return (
    <div className="">
      <FilterIcon />
    </div>
  );
};
