import { FcList } from "react-icons/fc";

const Category = {
  name: "category",
  type: "document",
  icon: FcList,
  fields: [
    {
      name: "name",
      type: "string",
    },
  ],
};

export default Category;
